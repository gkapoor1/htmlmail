<?php

/**
 * @file
 * Default template for HTML Mail
 *
 * DO NOT EDIT THIS FILE. Copy it to your theme directory, and edit the copy.
 *
 * When formatting an email message, Drupal determines the active template
 * directory by looking for the 'htmlmail.tpl.php' in the following locations:
 * - path_to_theme()
 * - drupal_get_path('module', $installed_profile)
 * - drupal_get_path('module', 'htmlmail')
 *
 * Once the active template directory is found, Drupal looks in that directory
 * for template files in order from most specific to most general.
 *
 * For example, if foo_module sends mail with:
 *   drupal_mail("foo_module", "outgoing_message" ...),
 * the possible template file names would be:
 * - htmlmail-foo_module_outgoing_message.tpl.php
 * - htmlmail-foo_module_outgoing.tpl.php
 * - htmlmail-foo_module.tpl.php
 * - htmlmail.tpl.php
 *
 * The $theme_hook_suggestions variable contains an array of suggested theme
 * hooks, in reverse priority order.  For the above example, it would contain:
 * - htmlmail
 * - htmlmail-foo_module
 * - htmlmail-foo_module_outgoing
 * - htmlmail-foo_module_outgoing_message
 *
 * For another example, to customize the password reset emails sent by the user
 * module, copy "htmlmail.tpl.php" to your theme directory, and then copy it to
 * "htmlmail-user_password_reset.tpl.php".  Remember that you will need to put
 * both files in your theme directory for this to work.
 *
 * Template files are cached, so remember to clear cache by visiting
 * admin/settings/performance after creating, copying, or editing any
 * template (.tpl.php) files.
 *
 * The following variables are also available in this template:
 * - $body: The message body text.
 * - $module: The sending module name, usually the first parameter to drupal_mail().
 * - $key: The message key, usually the second parameter to drupal_mail().
 * - $message_id: The email message id, usually "{$module}_{$key}".
 * - $debug: TRUE if debugging info should be printed.
 * - $directory: The relative path to the theme template directory.
 * - $theme_url: The absolute URL to the theme directory.
 *
 * The module calling drupal_mail() may set other variables.  For instance, the
 * webform module sets a $node variable which may be very useful.
 *
 * Other modules may also add or modify theme variables by implementing a
 * MODULENAME_preprocess_htmlmail() function.
 *
 * @see drupal_mail(), theme(), path_to_theme(), drupal_get_path()
 * @see template_preprocess_htmlmail(), hook_preprocess_HOOK()
 */
?>
<div class="htmlmail-body">
<?php print $body; ?>
</div>
<?php if ($debug): ?>
<pre class="htmlmail-debug">
Theme hook suggestions: <?php print_r($theme_hook_suggestions); ?>
</pre>
<?php endif; ?>
